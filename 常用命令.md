json文件的方法
# "methods": ["CAMP", "transformer+AMP", "oracle_AMP", "detected+LMMSE", "oracle_LMMSE"],
# 常用命令

## 激活虚拟环境

Windows PowerShell:

```powershell
.\.venv\Scripts\Activate.ps1
```

## 安装依赖

```powershell
pip install -r requirements.txt
```

## 训练网络

默认读取 `config.json` 里的 `network_train` 配置：

```powershell
python network\train.py
```

临时快速测试训练流程：

```powershell
python network\train.py --epochs 2 --steps_per_epoch 10 --batch_size 16
```

## 评估 PM/PF 曲线

默认读取 `config.json` 里的 `network_evaluate` 配置：

```powershell
python network\evaluate.py
```

临时指定 checkpoint：

```powershell
python network\evaluate.py --ckpt checkpoint\checkpoints_N200_Lp30_M32_bs128_steps2000\last.pt
```

## 检测 + 信道估计对比

默认读取 `config.json` 里的 `CE_methods_compare` 配置，方法列表由 `CE_methods_compare.methods` 决定：

```powershell
python CE_methods\compare.py
```

## 对比真实/预测活跃用户索引

默认读取 `config.json` 里的 `network_compare_active_indices` 配置：

```powershell
python network\compare_active_indices.py
```

## 画训练 loss 曲线

这个脚本不读取 `config.json`，参数从命令行传入：

```powershell
python plot\plot_loss_curve.py --log checkpoint\checkpoints_N200_Lp30_M32_bs128_steps2000\train.log --out loss_curve.png
```

## 画 AMP 迭代 NMSE 曲线

默认读取 `config.json` 里的 `plot_plot_amp_nmse_vs_iter` 配置：

```powershell
python plot\plot_amp_nmse_vs_iter.py
```

## 常改配置位置

`config.json` 顶层名字对应脚本：

```text
network_train                    network\train.py
network_evaluate                 network\evaluate.py
CE_methods_compare               CE_methods\compare.py
network_compare_active_indices   network\compare_active_indices.py
plot_plot_amp_nmse_vs_iter       plot\plot_amp_nmse_vs_iter.py
```

## 服务器后台训练命令

进入项目目录：

```bash
cd /data/wuxudong/correlated_activity_transformer
```

使用 GPU 2 后台训练，关闭 VSCode/SSH 后仍继续运行：

```bash
nohup bash -c 'CUDA_VISIBLE_DEVICES=2 python network/train.py' > train_2.out 2>&1 &
```

如果同时跑多张卡，输出文件要分开：

```bash
nohup bash -c 'CUDA_VISIBLE_DEVICES=0 python network/train.py' > train_0.out 2>&1 &
nohup bash -c 'CUDA_VISIBLE_DEVICES=1 python network/train.py' > train_1.out 2>&1 &
nohup bash -c 'CUDA_VISIBLE_DEVICES=2 python network/train.py' > train_2.out 2>&1 &
```

## 查看训练进程

查看当前用户的训练进程：

```bash
ps -fu wuxudong | grep "python network/train.py"
```

按 PID 查看指定进程：

```bash
ps -fp PID
```

查看 GPU 占用：

```bash
nvidia-smi
```

## 查看训练输出

实时查看 nohup 终端输出：

```bash
tail -f train_2.out
```

查看最近 50 行输出：

```bash
tail -n 50 train_2.out
```

搜索报错：

```bash
grep -i "error\|traceback\|cuda\|oom" train_2.out
```

## 查看 checkpoint 和 train.log

查看最新生成的 checkpoint 目录：

```bash
ls -td models/checkpoint_* | head -10
```

实时查看最新 checkpoint 的训练日志：

```bash
tail -f $(ls -td models/checkpoint_* | head -1)/train.log
```

查看某个指定 checkpoint 的训练日志：

```bash
tail -f models/checkpoint_xxx/train.log
```

查看某个 checkpoint 的最后几轮结果：

```bash
tail -n 20 models/checkpoint_xxx/train.log
```

## 停止训练

先查看训练进程 PID：

```bash
ps -fu wuxudong | grep "python network/train.py"
```

正常停止指定 PID：

```bash
kill PID
```

一次停止多个 PID：

```bash
kill PID1 PID2 PID3
```

如果普通停止无效，再强制停止：

```bash
kill -9 PID
```
